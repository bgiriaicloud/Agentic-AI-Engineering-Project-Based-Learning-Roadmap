apiVersion: v1
kind: Service
metadata:
  name: ai-inference-app-service
  labels:
    app: ai-inference-app
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
  selector:
    app: ai-inference-app
